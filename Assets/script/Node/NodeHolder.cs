using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodeHolder : MonoBehaviour
{
    /*
    [SerializeField] GameObject leftClick;
    [SerializeField] Vector3 offset;
    [SerializeField]private RectTransform rectTransform;


    private void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            Vector3 worldPosition = Camera.main.ScreenToWorldPoint(Input.mousePosition);
            rectTransform.position = worldPosition;
            //var canvasRect = GetComponent<RectTransform>();
            //Vector2 canvasRectHalf = new Vector2(canvasRect.rect.width / 2, canvasRect.rect.height / 2);
            //rectTransform.anchoredPosition = (Vector2)Input.mousePosition - canvasRectHalf;
        }
    }

    private void OnMouseDown()
    {
        Debug.Log("is click");
    }
    private void OnMouseUp()
    {
        
    }
    */
}
